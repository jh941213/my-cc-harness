# 런북 템플릿

```markdown
# Runbook: {알림명}

- 알림: {알림 룰 이름 + 링크}
- 심각도: {P1/P2/P3} — 사용자 영향: {무엇이 안 되는가}
- 최종 검토: {YYYY-MM-DD} / 담당: {팀/역할}

## 증상

- {사용자/시스템 관점에서 관측되는 것}

## 진단 (순서대로)

1. 대시보드 확인: {대시보드 링크}
   - 정상 기준선: {지표 X < N}
2. 로그 쿼리 (복붙):
   ```
   {실제 실행 가능한 쿼리}
   ```
3. 판별:
   - {패턴 A가 보이면} → 원인 후보 1 → 완화 §1
   - {패턴 B가 보이면} → 원인 후보 2 → 완화 §2
   - 둘 다 아니면 → 에스컬레이션

## 완화 (Mitigation)

### 1. {원인 후보 1일 때}
```bash
{복붙 가능한 명령}
```
⚠️ 트레이드오프: {이 조치의 부작용 — 예: 캐시 초기화로 5분간 지연 증가}

### 2. {원인 후보 2일 때}
```bash
{복붙 가능한 명령}
```

## 확인

- {완화가 성공했음을 확인하는 지표/명령}

## 근본 해결 / 후속

- [ ] {완화 후 반드시 해야 할 것 — 티켓 생성 기준}

## 에스컬레이션

- {N분} 내 해결 안 되면 → {누구에게, 어떤 채널로}
- 관련 문서: {아키텍처/배포 가이드 링크}
```
